// DEPRECATED: This helper was only used during one-off Mercy scoping and is now inert.
// It remains as a placeholder to avoid breaking imports if any existed. Safe to delete.
import 'dotenv/config';

console.log('scope-mercy-to-new-church.ts is deprecated and no longer in use.');
process.exit(0);
